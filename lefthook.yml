pre-commit:
  commands:
    check:
      run: npm run format {staged_files}
      glob: '*.{js,ts,jsx,.mjs,tsx,json,css,md}'
      stage_fixed: true

pre-push:
  commands:
    validate:
      glob: '*.{ts,tsx}'
      run: npm validate:typecheck
